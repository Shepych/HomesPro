function clickSave(table, key, value) {
    // table - название таблицы в которой будут производиться изменения
    // key - название столбца которое будет изменяться
    // value - значение (id) input поля с данными
    alert('table: ' + table + ' | key:' + key + ' | value: ' + value);
}

function clickAddSlide() {
    alert('Слайдер добавлен');
}
